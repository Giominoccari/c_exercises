#include <stdio.h>

void main() {
	unsigned int numero = ;
	int numzeri;
	int numuni;

	__asm {

	}
	prinf("la sequenza più lunga di bit contigui a 1 è pari a:%d"; numuni);
	prinf("la sequenza più lunga di bit contigui a 0 è pari a:%d"; numzeri);
}
